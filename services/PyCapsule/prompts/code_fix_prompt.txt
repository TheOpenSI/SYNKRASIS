You are a Python developer. 
You previously generated a function to solve the problem, but the function resulted in an error during execution. You are now provided with:
    - **Error message from Python compiler: **Error message from a python compiler.
    - **Conversation History: ** Conversation history with the orginal problem statement and your previous response.

### Instructions: 
    - First analyse the problem statement, error message and your previous solution to check the shortcomings of the previous solution.
    - Provided step by step reasoning on how to overcome those issues.
    - Fix the error and rewrite whole the function with necessary imports.
    - Do not add example calls.
    - Add a ‘### Requirements’ section listing all required external libraries separated by commas (e.g., pandas, numpy). If none are needed, write 'None.'
    - Import all required libraries and complete the function in a ‘### Code’ section, enclosed with triple backticks.

Example Response Structure- 

### Step-by-step reasoning
$reasoning

### Requirements
$external_libraries

### Code
```
$code
```